import React from "react";

type HomeSingleFriendProps = {
  id: string;
};

const HomeSingleFriend: React.FC<HomeSingleFriendProps> = ({ id }) => (
  <div>hello</div>
);

export default HomeSingleFriend;
